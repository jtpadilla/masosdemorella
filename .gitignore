# IDE y sistema
.idea/
.vscode/
.DS_Store
Thumbs.db

# Derivados de extract/extract.sh: se regeneran a partir del PDF (son pesados).
# render/ = PNG de cada página (referencia visual); images/ = JPEG en bruto (los buenos están en assets/images/)
extract/render/
extract/images/

# El proyecto Astro tiene su propio .gitignore en site/ (dist, node_modules, .astro, logs)
